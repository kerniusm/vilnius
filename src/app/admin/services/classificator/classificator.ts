export interface ClassificatorData {
  id: string;
  articile_code: number;
  articile_title: string;
  group_code: number;
  service_group: string;
  service_code: number;
  service_title: string;
}
